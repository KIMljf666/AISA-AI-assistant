# AIRI × NarrativeEngine 自驱系统集成 — 任务实施记录

> **用途**: 当对话窗口 token 受限后，新 AI agent 读取此文档即可快速定位全貌。
> **最后更新**: 2026-03-10T17:13:00+08:00 · **Phase 7-9 实施中 (7项增强任务)**

---

## 一、项目目标

将 NarrativeEngine 的自驱 AI 系统（9层意识流、L1-L4记忆、四层心理、RGES反思演化、通用感知）集成到 AIRI VTuber 角色"赛希斯"(Saihisis, ENTJ) 上。

**LLM 双模型方案**:
- **日常对话**: `moonshot-v1-auto` (Moonshot API, ~2-5s)
- **深度推理**: `kimi-k2.5` (temperature=1, ~8-30s, 含 reasoning_content)

**TTS 方案**:
- **引擎**: edge-tts (微软神经网络语音, 免费, 零延迟)
- **声线**: XiaoxuanNeural (晓萱, 优雅知性 — 赛希斯专属)
- **端点**: `POST /v1/audio/speech` (OpenAI 兼容)

---

## 二、当前架构

```
AIRI 前端 (Vue/Pinia, port 5200, strictPort)
  ├── chat.ts → fetchEnrichedPrompt() + submitDialogueFeedback()
  ├── narrative-bridge.ts → HTTP/WS 通信 + 双模型 + config CRUD + 🆕 proactive_dialogue
  ├── Stage.vue → TTS 播放 + 音量驱动 fallback 口型同步
  ├── Model.vue → Live2D 渲染 + 空闲口型抑制(说话时)
  └── CognitiveBubble.vue v2.0 → 4 Tab: 📡实时流 / 🪪身份卡 / 🎭角色编辑 / ⚙️设置
       │  🆕 因果连线 + 记忆溯源展开 + 记忆涟漪动画 + 主动发言高亮
       │  HTTP GET/POST/PUT + WebSocket
       ▼
airi_bridge.py (FastAPI 桥接层, port 5555)
  ├── /character-state/{id}/system-prompt → L0→L1→L2→L3→L5→L6 事件广播
  │     🆕 L3 记忆广播含 search_method + similarity scores + 完整记忆列表
  ├── /dialogue-feedback → L7 reasoning + L9 反思事件
  ├── /character-config/{id} → GET/PUT 角色配置 CRUD
  ├── /model-config → 双模型查询/切换
  ├── /v1/audio/speech → Edge-TTS 语音合成 (OpenAI 兼容)
  ├── /v1/audio/voices → 8种中文声线列表
  └── _get_provider() → CPI 协议层
       │
       ▼
api/cpi/ (认知供应商接口) — 完全可插拔
  ├── interface.py → ICognitiveProvider ABC (13 方法)
  ├── narrative_engine.py → NE 实现 (深度合并 + JSON persist)
  │     🆕 VectorMemoryIndex (sentence-transformers, 384维, cosine相似度)
  │     🆕 recall_memories() → 向量语义检索优先 + 关键词 fallback
  │     🆕 store_memory() → 同步到向量索引
  │     🆕 _extract_key_facts_llm() → Moonshot API 结构化提取
  └── real_world_perception.py → 真实世界感知
```

---

## 三、执行进度

### Phase 1-4: 基础→CPI→深度→意识流 ✅
### Phase 5a: 真实世界感知管道 ✅
### Phase 5b: 端到端验证 ✅
### Phase 5c: 一键启动 + 文档 ✅
### Phase 5d: Reasoning 优化 + CognitiveBubble 增强 ✅
### Phase 5e: 大五人格集成 ✅
### Phase 5f: 角色配置编辑器 (可插拔) ✅
### Phase 6: TTS + 口型同步 + 端口安全 ✅

### Phase 7: 记忆质量升级 ✅

**Task 1: 向量语义检索 ✅** (~2h)
- ✅ `VectorMemoryIndex` 类 (135行): sentence-transformers `paraphrase-multilingual-MiniLM-L12-v2`, 384维
- ✅ 懒加载模型 (全局单例) + pickle 持久化 (`data/vector_index/{char_id}.pkl`)
- ✅ `recall_memories()` 三级策略: 向量检索(阈值>0.3) → 关键词匹配 → 时间排序
- ✅ `store_memory()` 每次写入同步到向量索引
- ✅ 验证: "学生的名字" → "用户叫小明" 相似度 0.646 (3× 高于无关结果)

**Task 6: LLM 驱动 Key Fact 提取 ✅** (~1h)
- ✅ `_extract_key_facts_llm()`: Moonshot API (moon-v1-auto) 结构化提取
  - 提取维度: 姓名/偏好/事件/情绪/意图
  - JSON 格式约束 + 5秒超时
- ✅ `submit_experience()` 先 LLM → 失败降级到规则引擎
- ✅ 返回 `extraction_method` 字段 ("llm"|"regex"|"none")

### Phase 8: 认知透明 (深度设计) ✅

**Task 2: CognitiveBubble 静态/动态分离 ✅** (~40min)
- ✅ 🪪 **身份卡面板**: 角色头部 + MBTI/星座标签
- ✅ 大五人格 SVG 雷达图 (5维, 动态数据点)
- ✅ 四层心理卡片 (核心价值观/身份认同/动机/情绪) + 渐变色
- ✅ 目标进度条 (P1-P3 优先级 + 进度百分比)
- ✅ 需求仪表盘 (身体状态值条)
- ✅ 实时流与身份卡完全分离

**Task 3: 记忆溯源展开 ✅** (~20min)
- ✅ `memory_recall` 事件可点击展开
- ✅ 每条记忆显示: 层级标签(L2/L4) + 内容 + 相似度分数 + 时间
- ✅ 显示搜索方式徽章 (🔍 语义 / 📝 关键词)
- ✅ 后端 L3 广播携带完整 memories 列表 + search_method + top_score

**Task 4: 记忆涟漪动画 ✅** (~15min)
- ✅ `memory_recall` 事件触发 CSS 波纹动画 (fuchsia 色, 0.8s)
- ✅ 因果连线: 同批次事件紫色竖线连接 (CSS ::after)
- ✅ 主动发言事件高亮 (金色左边框 + 背景色)

### Phase 9: 主动性 ✅

**Task 5: 前端 proactive_dialogue ✅** (~1h)
- ✅ `CognitiveEvent` 接口增加 `proactive_dialogue` 类型
- ✅ `narrative-bridge.ts` 增加 `proactiveMessage` ref + `onProactiveDialogue` 回调
- ✅ WebSocket 处理器检测并分发主动对话事件
- ✅ CognitiveBubble 中主动发言事件特殊样式

### Phase 8.1: 前端稳定性修复 ✅

- ✅ **端口冲突永久修复**: 5174 → 5200, 远离 Genesis Arena (5173)
- ✅ **PWA SW 禁用**: `devOptions: { enabled: false }` 阻止 Service Worker 开发模式注册
- ✅ **WebSocket 重连刷屏修复**: `maxReconnectAttempts: 5` + 日志节流 (30s)
  - `channel-server.ts`: 6121 端口无服务时最多重试 5 次即停止

### Phase 10: 验证 (待执行)

- [ ] Task 7: RGES 目标演化验证 — 10 轮对话后验证目标自然演化

---

## 四、可插拔架构 & 可调配置项

| 组件       | 切换方法                                  |
| ---------- | ----------------------------------------- |
| 认知供应商 | 实现 ICognitiveProvider (13 方法)         |
| LLM 模型   | `/api/airi/model-config/switch`           |
| TTS 语音   | `/v1/audio/speech` voice 参数             |
| 角色配置   | `GET/PUT /api/airi/character-config/{id}` |
| 前端       | `bridgeUrl` / `characterId` 设置          |

---

## 五、启动方式

```bash
# 终端 1 — 后端 (port 5555)
cd /Users/jifanliu/Desktop/开端/后端/NPC\ system\ 20250328/Vtuber
./scripts/start_backend.sh

# 终端 2 — 前端 (port 5200 🆕, 自动清理端口冲突)
./scripts/start_frontend.sh
# 访问 http://localhost:5200/
```

### AIRI 前端配置
1. LLM: OpenAI 兼容 API → Key: `sk-J5VBF...` / Base: `https://api.moonshot.cn/v1` / Model: `moonshot-v1-auto`
2. TTS: OpenAI 兼容 Audio Speech → Key: `sk-test` / Base: `http://localhost:5555/v1` / Model: `edge-tts` / Voice: `xiaoxuan`

### Git 分支
- **NPC system 主仓库**: `feature/genesis-arena-mvp` (所有 NarrativeEngine + bridge 修改)
- **airi 子仓库**: `main` (上游 fork, 前端修改在此分支)

---

## 六、评分: 43/45 (95.6%)

| 维度     | 最终                                 |
| -------- | ------------------------------------ |
| 通用感知 | **5/5**                              |
| 意识流   | **5/5**                              |
| 记忆     | **4/5** → 🆕 向量语义检索已实装       |
| RGES     | **4/5**                              |
| 提示词   | **5/5**                              |
| 管道效率 | **5/5**                              |
| I/O效率  | **5/5** ← TTS + 口型同步             |
| 可视化   | **5/5** ← 🆕 身份卡 + 涟漪 + 因果连线 |
| 架构解耦 | **5/5**                              |

---

## 七、已知问题 & 经验

| 问题                            | 原因                                 | 解决                                |
| ------------------------------- | ------------------------------------ | ----------------------------------- |
| 端口 5174 被 Genesis Arena 抢占 | GA (5173) 的 Service Worker 互相干扰 | 🆕 改为 5200 + 禁用 PWA dev mode     |
| WASM lip sync 加载失败          | createLive2DLipSync 环境兼容性       | 音量驱动 fallback (Stage.vue)       |
| 空闲动画覆盖口型                | 多处写ParamMouthOpenY冲突            | 抑制空闲嘴巴曲线 + watcher跳过      |
| start_backend.sh路径错误        | narrative_engine不在Vtuber内         | 改为 `../narrative_engine/`         |
| WS 6121 重连刷屏                | 无 AIRI server 但无限重试            | 🆕 maxReconnectAttempts:5 + 日志节流 |

---

## 八、后续工作

1. **P3 验证**: 10 轮对话后验证 RGES 目标自然演化
2. **记忆积累验证**: 充分对话后测试向量语义检索 vs 关键词的效果差异
3. **LLM key fact 验证**: 验证 Moonshot API 提取质量 vs 规则引擎
4. **主动对话触发**: 后端 consciousness_loop 产生 proactive_dialogue 事件的条件调优

## 九、本轮 (Phase 7-9) 修改文件清单

| 文件                                           | 修改摘要                                                     |
| ---------------------------------------------- | ------------------------------------------------------------ |
| `narrative_engine/api/cpi/narrative_engine.py` | +VectorMemoryIndex (135行), +向量recall/store, +LLM key fact |
| `narrative_engine/api/airi_bridge.py`          | L3广播增强 (search_method/score/memories)                    |
| `airi/.../CognitiveBubble.vue`                 | v2.0 重写 (身份卡/雷达图/溯源/涟漪/因果连线)                 |
| `airi/.../narrative-bridge.ts`                 | +proactive_dialogue 类型/处理                                |
| `airi/.../channel-server.ts`                   | +maxReconnectAttempts:5 + 日志节流                           |
| `airi/.../vite.config.ts`                      | port 5200 + PWA devOptions disabled                          |
| `Vtuber/scripts/start_frontend.sh`             | port 5200                                                    |

## 十、Phase 10: 后端测试基础设施 (2026-03-13)

### 目标
建立完整的后端自动化测试覆盖，在前端实现前验证核心链路。

### 测试结果: 50/50 PASSED ✅

| 测试套件                    | 测试数 | 通过 | 耗时    |
| --------------------------- | ------ | ---- | ------- |
| `test_cpi_interface.py`     | 35     | 35   | ~20s    |
| `test_airi_bridge.py`       | 15     | 15   | ~13s    |
| **合计**                    | **50** | **50** | **~25s** |

### 关键验证点

1. **Mock 14 方法契约**: 全部返回合法类型，降级策略有效
2. **NE Provider 核心能力**: 配置加载 / 感知 / 人格 / 增强prompt / 记忆 — 全部正常
3. **Singleton 缓存**: UCS (per-char) / GLM / ERE 实例复用确认
4. **向量语义检索**: MPS 加速，最高相似度 0.5477
5. **桥接 API 全端点**: health / character-state / system-prompt / dialogue-feedback / character-config / cognitive-log — 全通
6. **接口解耦**: Mock & NE 共享 14 方法 ABC 接口，一致性 100%

### 发现并修复的问题

| 问题                        | 原因                                          | 解决                                |
| --------------------------- | --------------------------------------------- | ----------------------------------- |
| `api.cpi` ModuleNotFoundError | `NPC system 20250328/api/` 覆盖 `narrative_engine/api/` | conftest 清除路径冲突 + 注册正确包  |
| Flask import 失败           | `api/__init__.py` 依赖 Flask (game API)       | conftest mock Flask 注入 sys.modules |
| dialogue-feedback 422       | 字段名 `assistant_message` vs `assistant_response` | 修正测试数据字段名                  |
| DHM add_dialogue 不存在     | DialogueHistoryManager 接口变更               | 文件降级存储 (已有 fallback)         |

### 新增文件

| 文件                                                   | 说明                        |
| ------------------------------------------------------ | --------------------------- |
| `narrative_engine/conftest.py`                         | 根级 pytest 配置: Flask mock + 路径修复 |
| `tests/integration/conftest.py`                        | 集成测试层 conftest          |
| `tests/integration/test_cpi_interface.py`              | CPI 契约+能力+缓存+工厂测试 (35 tests) |
| `tests/integration/test_airi_bridge.py`                | 桥接 API 端点+管道+解耦测试 (15 tests) |

