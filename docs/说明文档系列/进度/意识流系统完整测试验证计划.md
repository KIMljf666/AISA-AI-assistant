# 意识流自驱系统 — 完整测试验证计划

> 编制日期: 2026-03-13 | 版本: v1.0  
> 目标: 在后端完成核心验证后再推进前端实现，确保意识流全链路稳定可靠

---

## 一、测试分层架构

```
┌─────────────────────────────────────────────────────┐
│  L5: E2E 对话验证 (LLM 实际调用 + 多轮闭环)         │ ← P2
├─────────────────────────────────────────────────────┤
│  L4: 意识流管道验证 (UCS→GLM→ERE 完整链路)          │ ← P1
├─────────────────────────────────────────────────────┤
│  L3: Bridge API 集成 (FastAPI TestClient 全端点)    │ ← P0 ✅
├─────────────────────────────────────────────────────┤
│  L2: CPI 契约测试 (14方法 × 2实现 + Singleton)     │ ← P0 ✅
├─────────────────────────────────────────────────────┤
│  L1: 单组件单元测试 (向量索引/规则引擎/记忆CRUD)    │ ← P0 ✅
└─────────────────────────────────────────────────────┘
```

---

## 二、当前进度 (P0 已完成)

| 层级 | 测试文件 | 测试数 | 状态 |
|------|---------|--------|------|
| L1-L2 | `test_cpi_interface.py` | 35 | ✅ 35/35 PASSED |
| L3 | `test_airi_bridge.py` | 15 | ✅ 15/15 PASSED |
| L4 | `test_p1_pipeline.py` | 22 | ✅ 18 PASSED + 4 SKIPPED (API) |
| L5 | `test_e2e_dialogue.py` | 14 | ✅ 14/14 PASSED |

---

## 三、P1 阶段计划: 意识流管道深度验证

### 3.1 LLM 关键信息提取测试 (`test_llm_key_fact_extraction.py`)

**目标**: 验证 L2/L3 记忆层的 LLM 驱动提取能力

**LLM API 优先级**:
1. 🥇 Google Gemini (`gemini-2.0-flash`) — 通过 OpenAI 兼容接口调用
2. 🥈 阿里云百炼 (`qwen3.5-plus`)
3. 🥉 Moonshot (`moonshot-v1-auto`) — 现有实现

**测试用例**:

| # | 测试场景 | 输入 | 期望提取 |
|---|---------|------|---------|
| 1 | 姓名提取 | "我叫小明" | `用户姓名: 小明` |
| 2 | 偏好提取 | "我最喜欢吃披萨和拉面" | `用户偏好: 披萨`, `用户偏好: 拉面` |
| 3 | 关系提取 | "我妹妹叫小红" | `用户关系: 妹妹小红` |
| 4 | 情绪提取 | "今天考试没过，很沮丧" | `用户情绪: 沮丧` |
| 5 | 无信息 | "今天天气真好" | `[]` (空列表) |
| 6 | 复合信息 | "我叫张三，喜欢蓝色，讨厌下雨" | ≥3条提取 |
| 7 | 降级验证 | API 不可用时 | 降级到规则引擎 |

**验收标准**: 6/7 场景通过，提取准确率 ≥ 80%

### 3.2 意识流管道完整性测试 (`test_consciousness_pipeline.py`)

**目标**: 验证 UCS 9层因果链 → GLM 目标管理 → ERE 反思 的完整管道

**测试用例**:

| # | 测试场景 | 验证点 |
|---|---------|--------|
| 1 | UCS 意识流生成 | 给定刺激，UCS 应生成包含感知→认知→决策的分层响应 |
| 2 | UCS 状态持久化 | 同一角色两次调用 `get_consciousness()` 应反映状态延续 |
| 3 | GLM 目标生命周期 | 创建目标→执行→完成/放弃 的完整周期 |
| 4 | GLM 目标优先级排序 | 多目标场景下优先级排序正确 |
| 5 | ERE 经验提交 | submit_experience 后 ERE 应产生反思洞察 |
| 6 | ERE RGES 触发 | 含 reasoning_content 的经验应触发 RGES |
| 7 | 管道联动 | 刺激→UCS处理→GLM目标调整→ERE反思 全链路 |
| 8 | 主动对话触发 | consciousness_loop 产生 proactive_dialogue 事件 |

**验收标准**: 核心链路 (1,2,5,7) 必须通过

### 3.3 记忆系统深度验证 (`test_memory_deep.py`)

**目标**: 验证向量语义检索 vs 关键词检索的效果差异

**测试用例**:

| # | 测试场景 | 验证点 |
|---|---------|--------|
| 1 | 精确匹配 | "小明" → 包含"小明"的记忆 |
| 2 | 语义匹配 | "学生叫什么" → 包含"小明"的记忆 (语义关联) |
| 3 | 跨轮累积 | 5轮对话后，recall 应返回跨轮信息 |
| 4 | L2 升级验证 | key_fact 提取后存入 L2，优先级高于 L4 |
| 5 | 记忆衰减 | 旧记忆分数低于新记忆 |
| 6 | 大量记忆性能 | 100+ 记忆下检索耗时 < 500ms |

**验收标准**: 语义匹配 (2) 召回率 ≥ 60%，性能 (6) 达标

---

## 四、P2 阶段计划: E2E 对话闭环验证

### 4.1 端到端对话验证 (`test_e2e_dialogue.py`)

**目标**: 真实 LLM 调用下的完整对话体验验证

| # | 测试场景 | 验证点 |
|---|---------|--------|
| 1 | 首轮对话 | 角色回复符合人设 (ENTJ 风格) |
| 2 | 记忆召回 | 第 N 轮提到第 1 轮的信息时角色能回忆 |
| 3 | 人格一致性 | 10轮对话后性格不漂移 |
| 4 | 主动发言 | proactive_dialogue 事件触发角色主动开口 |
| 5 | reasoning 传递 | 前端→后端 reasoning_content 完整到达 RGES |
| 6 | 认知事件广播 | WebSocket cognitive_stream 事件层级覆盖 ≥ 5 |

### 4.2 前端闭环验证 (手动)

| # | 验证步骤 | 预期结果 |
|---|---------|---------|
| 1 | 打开 AIRI 前端，与赛希斯对话 | 角色正常回复 |
| 2 | 观察后端日志 reasoning | 控制台打印 `[推理过程]: {think内容}` |
| 3 | 等待主动对话触发 | 前端自动显示角色主动发言 |
| 4 | 关闭后重开，继续对话 | 角色记得之前的对话内容 |

---

## 五、LLM API 配置

**优先级**:

| 优先级 | 供应商 | 模型 | 接口 | 状态 |
|--------|--------|------|------|------|
| 🥇 1 | DeepSeek | deepseek-chat | OpenAI兼容 | ✅ 已验证 |
| 🥈 2 | Google Gemini | gemini-2.0-flash | OpenAI兼容 | ⚠️ 429 rate limit |
| 🥉 3 | 阿里云百炼 | qwen-plus | DashScope | ⚠️ 401 auth |
| 4 | Moonshot | moonshot-v1-auto | OpenAI兼容 | 本地已有 |

**降级策略**: DeepSeek → Google → 阿里云 → Moonshot → 规则引擎

---

## 六、执行时间表

| 阶段 | 时间 | 内容 | 状态 |
|------|------|------|------|
| ~~P0~~ | Day 1 | CPI + Bridge 测试 50/50 | ✅ 完成 |
| ~~P1~~ | Day 2 | LLM API + 意识流管道 + 记忆 20/22+2skip | ✅ 完成 |
| ~~P2~~ | Day 2 | E2E 对话验证 14/14 | ✅ 完成 |
| ~~P3~~ | Day 2 | 50轮长会话 Benchmark 11/11 | ✅ 完成 |
| **P4** | Day 3 | 行为 Pipeline 接入 + RGES 闭环 | 🔲 待实施 |

---

## 七、P3 结果摘要 (50轮 Benchmark)

| 指标 | 结果 |
|------|------|
| 人格一致性 (ENTJ) | **9/10** — "始终维持直接果断与领导力" |
| 压力韧性 | **9/10** — "保持沉稳，未失控或过度道歉" |
| AI 自称违规 | **0处** (50轮) |
| 记忆召回率 | **86%** (6/7 关键事实命中) |
| 姓名检索 similarity | **0.8449** |
| Prompt 增长 | 611→961 chars (受控) |
| LLM 错误率 | **0%** |

---

## 八、P4 行为 Pipeline 接入验证 (计划)

> 核心目标: 让 AIRI 对话走已有的行为 pipeline，打通 RGES 反思闭环

### P4 测试项

| ID | 测试内容 | 验证点 |
|----|---------|--------|
| P4-1 | UCS 刺激接入 | `process_stimulus` 后 consciousness ≠ None |
| P4-2 | 对话意图提取 | SOCIAL_INTERACT ActionType 正确生成 |
| P4-3 | 回复质量评估 | IntentVerification quality ∈ [0,1] |
| P4-4 | RGES moment 记录 | `moment_collected=True` |
| P4-5 | 条件反思触发 | quality < 0.5 时 reflection_triggered=True |
| P4-6 | 反思回流 prompt | 下一轮 system_prompt 含"近期反思"区段 |
| P4-7 | 50轮 RGES 闭环 | reflections > 0 且有意义 |

### P4 前置条件
- `pip install aiosqlite` (解除 GLM 阻塞)
- submit_experience 中接入 `process_stimulus`
- RGES `begin_moment → record_actions → finalize_and_reflect` 三步接口连通

---

## 九、运行命令

```bash
cd narrative_engine

# P0 (50/50 ✅)
python -m pytest tests/integration/test_cpi_interface.py tests/integration/test_airi_bridge.py -v -s

# P1 (20/22+2skip ✅)
python -m pytest tests/integration/test_p1_pipeline.py -v -s

# P2 (14/14 ✅)
python -m pytest tests/integration/test_e2e_dialogue.py -v -s

# P3 (11/11 ✅, ~6min)
python -m pytest tests/integration/test_long_session_benchmark.py -v -s --timeout=600

# 全量 P0-P2 (86 tests, ~67s)
python -m pytest tests/integration/test_cpi_interface.py tests/integration/test_airi_bridge.py tests/integration/test_p1_pipeline.py tests/integration/test_e2e_dialogue.py -v --tb=short -s
```

